# Интеграция MaxUI в проект Zenith

## ✅ Выполненные работы

### 1. Установка библиотеки
```bash
npm install @maxhub/max-ui --legacy-peer-deps
```

### 2. Интегрированные компоненты MaxUI

#### В App.tsx:
- `MaxUI` - провайдер приложения
- `Panel` - для обёртки контента placeholder-экранов
- `Button` - для FAB кнопки

#### В TaskFormOrganism.tsx:
- `Panel` - для модального окна формы
- `Input` - для полей ввода названия и даты
- `Textarea` - для поля описания
- `Button` - для кнопок приоритета и submit

#### В TaskListScreen.tsx:
- `Button` - для кнопок фильтров (Все, Сегодня, Истекающие)

#### В TaskItem.tsx:
- `IconButton` - для кнопки удаления задачи

### 3. Кастомизация стилей

Создан файл `src/maxui-overrides.css` с переопределением стилей MaxUI под концепцию Zenith:

- **CSS переменные**: Переопределены основные цвета MaxUI
- **Кнопки**: Настроены primary и secondary режимы с цветами Zenith
- **Панели**: Применён цвет surface-gray и тени
- **Инпуты**: Тёмный фон с фиолетовым акцентом при фокусе
- **IconButton**: Настроены hover эффекты

### 4. Структура импортов

```typescript
// main.tsx
import '@maxhub/max-ui/dist/styles.css';
import './index.css';
import './maxui-overrides.css';
```

**Важно**: Порядок импортов критичен:
1. Сначала стили MaxUI
2. Затем базовые стили приложения
3. В конце переопределения

### 5. Сохранённая визуальная концепция Zenith

Все компоненты MaxUI адаптированы под палитру Zenith:
- Фиолетовый акцент (`#7E6FD5`)
- Жёлтый для FAB (`#FFE552`)
- Тёмный фон (`#121218`)
- Серые поверхности (`#1F1E24`)

## 📝 Рекомендации

1. **Запуск dev сервера**:
   ```bash
   npm run dev
   ```

2. **Сборка проекта**:
   ```bash
   npm run build
   ```

3. **Расширение функционала**:
   - Используйте другие компоненты MaxUI при необходимости
   - Все переопределения стилей добавляйте в `maxui-overrides.css`
   - Следуйте паттерну data-attributes для селекторов: `[data-max-ui="ComponentName"]`

## 🎨 Доступные компоненты MaxUI

Из библиотеки доступны:
- Avatar
- Button / IconButton / ToolButton / SvgButton
- Input / ClearableInput / SearchInput
- Textarea
- Panel
- Container
- Flex / Grid
- Typography
- Switch
- Counter
- Dot
- Spinner
- Tappable
- Cell* компоненты (CellList, CellSimple, CellHeader и др.)

## 🔗 Полезные ссылки

- [Storybook MaxUI](https://max-messenger.github.io/max-ui)
- [NPM Package](https://www.npmjs.com/package/@maxhub/max-ui)
- [GitHub Repository](https://github.com/max-messenger/max-ui)

